import * as React from 'react';

const Spinner = () => {
  return (
    <div className="inline-block h-6 w-6 animate-spin rounded-full border-3 border-t-slate-950 border-r-transparent border-b-transparent border-l-transparent"/>
  );
};

export default Spinner;
